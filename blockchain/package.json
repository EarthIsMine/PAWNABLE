{
  "name": "@pawnable/blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "Smart contracts for PAWNABLE NFT lending platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "node": "hardhat node"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.10.5",
    "hardhat": "^2.22.18",
    "typescript": "^5.7.2"
  }
}
